name: Test Examples

on: [push]

jobs:
  conda:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install treebeard
      - name: Add credentials
        run: treebeard configure --email robot@treebeard.io
      - name: Check credentials
        run: |
          cat ~/.treebeard

  virtualenv:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install treebeard
      - name: Add credentials
        run: treebeard configure --email robot@treebeard.io
      - name: Check credentials
        run: |
          cat ~/.treebeard
      - name: Test
        run: |
          cd examples/virtualenv
          treebeard run --watch --confirm

  dagster:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install treebeard
      - name: Add credentials
        run: treebeard configure --email robot@treebeard.io
      - name: Check credentials
        run: |
          cat ~/.treebeard
      - name: Test
        run: |
          cd examples/dagster
          treebeard run --watch --confirm

  failing_notebook:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install treebeard
      - name: Add credentials
        run: treebeard configure --email robot@treebeard.io
      - name: Check credentials
        run: |
          cat ~/.treebeard
      - name: Test
        run: |
          cd examples/failing_notebook
          python -m run_test

  reddit_tracker:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip	
          pip install treebeard
      - name: Add credentials
        run: treebeard configure --email robot@treebeard.io
      - name: Check credentials
        run: |
          cat ~/.treebeard
      - name: Test
        run: |
          cd examples/reddit_tracker	
          treebeard run --watch --confirm_no_secrets

  hello_treebeard:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip	
          pip install treebeard
      - name: Add credentials
        run: treebeard configure --email robot@treebeard.io
      - name: Check credentials
        run: |
          cat ~/.treebeard
      - name: Test
        run: |
          cd examples/hello_treebeard	
          treebeard run --watch --confirm_no_secrets
